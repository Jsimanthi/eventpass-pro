module.exports = {
  plugins: [
    // PostCSS modules disabled to use regular CSS classes
    // require('postcss-modules')({
    //   generateScopedName: '[name]__[local]___[hash:base64:5]',
    //   exportGlobals: true,
    //   localsConvention: 'camelCase',
    // }),
  ],
};